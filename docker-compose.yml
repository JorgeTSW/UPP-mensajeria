version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:80"
    volumes:
      - ~/.aws:/root/.aws:ro  # Mapeo de credenciales (Solo lectura)
    environment:
      - AWS_PROFILE=upp-practica
